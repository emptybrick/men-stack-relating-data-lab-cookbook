<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Recipe</title>
</head>
<nav>
    <a href="/recipes">Back</a>
</nav>

<body>
    <h3>Update Recipe</h3>
    <form action="/recipes/<%= recipe._id %>?_method=PUT" method="POST">
        <label for="name">Recipe Name:</label>
        <input type="text" name="name" id="name" value="<%= recipe.name %>" />

        <label for="instructions">Instructions:</label>
        <textarea name="instructions" id="instructions" rows="4" cols="50"><%= recipe.instructions %></textarea>
        <h3>Ingredients</h3>
        <% if(allIngredients) { allIngredients.forEach(ingredient=> { %>
            <input type="checkbox" id="ingredients" name="ingredients" value="<%=ingredient._id %>" <%= ingredients.find(ing => ing._id.equals(ingredient._id)) ? 'checked' : '' %>>
            <label for="ingredients">
                <%=ingredient.name %>
            </label>
            <% }); %>
                <% } %>
                    <a href="/ingredients"><button type="button">New Ingredient</button></a>
                    <div><button type="submit">Update Recipe</button></div>
    </form>
</body>

</html>